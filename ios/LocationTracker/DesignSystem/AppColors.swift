import SwiftUI

enum AppColors {
  static let background = Color(red: 0.15, green: 0.15, blue: 0.15)
  static let surface = Color.white
  static let primaryText = Color.white
  static let secondaryText = Color(red: 0.79, green: 0.79, blue: 0.79)
  static let googleBlue = Color(red: 0.26, green: 0.52, blue: 0.96)
  static let ink = Color(red: 0.15, green: 0.15, blue: 0.15)
}
