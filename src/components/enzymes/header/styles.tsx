import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  parentView: {
    flex: 1,
    flexDirection: 'row',
  },
  viewFlex: {
    flex: 1,
  },
  textStyle: {
    color: 'red',
    fontSize: 12,
    textAlign: 'center',
  },
});
